# Environment variable overrides for Docker Compose
# Copy this to .env and customize before running

# MySQL credentials
DB_ROOT_PASSWORD=root123
DB_USERNAME=stmuser
DB_PASSWORD=stmpass

# JWT Secret — CHANGE THIS IN PRODUCTION (must be 32+ characters)
JWT_SECRET=SuperSecretKey_ChangeInProd_256BitsMinimum!!
